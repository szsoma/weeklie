### Fixes
1. remove the borders from the active Add task input.
2. remove borders from all task rows. and should be rounded, even the hover and active states.
3. remove all placeholder empty task rows from daycolumns
4. All task rows should have the same height, even when empty, filled, active, or hovered.
5. All column headers should have the same height, Backlog title row too.
6. Create the about page and link it from the navbar. Write a brief description of the project.
7. Update the Readme file too.
8. The user should be able to edit only the next task row
9. the app should play a subtle chime when a task is completed, or when a task is added.
10. the dragged task should be rounded too. should be black background with white text with subtle large shadow.
11. the day number and month should be bold and same color, the day name should be lighter and sentence case
12. On mobile I only want to see the navbar when I'm on the top of the page. Slide out when the user scrolls down in the week view. and reveal again when the user scrolls to the top to monday
13. Use more contrast in the app
